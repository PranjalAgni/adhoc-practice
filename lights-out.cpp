#include <bits/stdc++.h>
using namespace std;

int main() {
#ifndef ONLINE_JUDGE
	// for getting input from input.txt
	freopen("input.txt", "r", stdin);
	// for writing output to output.txt
	freopen("output.txt", "w", stdout);
#endif

	vector<vector<int>> states(3)
	for (int idx = 0; idx < 3; idx++) {
		int a, b, c;
		cin >> a >> b >> c;
		states[idx].push_back({a, b, c});
	}

	for (int idx = 0; idx < 3; idx++) {

	}

	return 0;
}